<h1><%= @user.fname %> <%= @user.lname %></h1>
<br>
<a href="<%= new_project_url %>">Create New Project</a>
<br>
<% @projects.each do |project| %>
  <h1><a href="<%= project_url(project.id) %>"><%= project.title %></a></h1>
  <p>
    <%= project.description %>
  </p>
  <br>
<% end %>
